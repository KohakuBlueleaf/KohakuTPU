# Simulation target 2: one system node, one router, one matmul cluster, one
# vector core. The node's control processor is INSIDE the node at (0,0) and
# costs no attach point, so nothing here places it.
#
# Five attach points on a 1x1 grid (four edges plus the router local); two are
# `nul` because only three are wanted.
xxx nul xxx
mag mat vec
xxx nul xxx
