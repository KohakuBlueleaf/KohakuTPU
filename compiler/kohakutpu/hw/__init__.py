"""What the silicon is: the vector ISA, the operand layout, the MX format.

Moved here from the retired `src/ktpu` when that tree went. Only the parts
the compiler actually reads came across; the rest is in git history.
"""
